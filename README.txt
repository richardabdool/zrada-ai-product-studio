ZRADA AI Product Studio v2.5 — RESILIENT BULK ENGINE

BUILT ON THE WORKING v2.3 SERVER-KEY/CATEGORY SYSTEM.

WHAT v2.5 FIXES
- Handles Render/hosting HTML error pages safely instead of showing:
  Unexpected token '<', '<!DOCTYPE...' is not valid JSON
- Automatically retries temporary failures up to 3 attempts.
- Backoff waits between retries: 5 seconds, then 10 seconds.
- Adds a 2.5-second controlled delay between successful image jobs.
- Adds RETRYING status in the queue.
- Adds Retry Failed Only button.
- Successful jobs are never regenerated by Retry Failed Only.
- A job that succeeds after a retry clears its old error message.
- Keeps approved/downloaded image clearing from v2.3.
- Preserves the server-side OPENAI_API_KEY setup.
- Preserves category-specific prompts and same-style reference locking.
- Adds an 8-minute server timeout so a stuck OpenAI request can recover cleanly.

DEPLOY
Upload the contents of this ZIP to the SAME GitHub repository and replace the current files.
Commit directly to main.
Render should auto-deploy.
Your existing OPENAI_API_KEY Render environment variable remains in place.

RECOMMENDED BULK WORKFLOW
1. Upload parent folder.
2. Add to Queue.
3. Generate Images.
4. Temporary failures will retry automatically.
5. If anything remains FAILED, click Retry Failed Only.
6. Review and approve.
7. Download Approved ZIP.
8. Clear Approved.


V2.5 IMAGE MIME FIX
- Detects JPEG/PNG/WebP from the actual uploaded file bytes.
- Genuine JPEG files incorrectly labelled image/dng are sent to OpenAI as image/jpeg.
- Existing category engine, consistency locking, retry system, approvals, ZIP export and Clear Approved remain unchanged.
